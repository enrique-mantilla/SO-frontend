export class User {
  constructor({ id, nombre, usuario, email, contrasena, bio, fechaRegistro }) {
    this.id = id
    this.nombre = nombre
    this.usuario = usuario
    this.email = email
    this.contrasena = contrasena
    this.bio = bio
    this.fechaRegistro = fechaRegistro
  }
}
